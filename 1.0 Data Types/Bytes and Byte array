Python bytearray() The bytearray() method returns a bytearray object which is an array of the given bytes. 
The bytearray() method returns a bytearray object which is a mutable (can be modified) sequence of integers in the range 0 <=x < 256. 
If you want the immutable version, use bytes() method.

lst=[10,20,30,244]
print(type(lst))
b=bytes(lst)    #from list to bytes
print(type(b))
#b[3]=33 ; not supported
b1=bytearray(lst)   #from list to bytearray
print(type(b1))
print(b1)
b1[3]=33
print(b1)
